<Window x:Class="Spacy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <ListBox Grid.Row="0" Grid.Column="0" ItemsSource="{Binding Path=DiskStatus}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="3" Margin="3" Padding="5" Background="Beige">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition></RowDefinition>
                                <RowDefinition></RowDefinition>
                                <RowDefinition></RowDefinition>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="{Binding Path=DriveNameText}" Margin="3"></TextBlock>
                            <TextBlock Grid.Row="1" Text="{Binding Path=AvailableFreeSpaceText}" Margin="3"></TextBlock>
                            <DockPanel Grid.Row="2">
                                
                                <Rectangle Width="{Binding Path=TotalSpaceIndicator.Width}" 
                                           Fill="{Binding Path=TotalSpaceIndicator.Fill}"
                                           Height="25" Margin="3,3,0,3">
                                </Rectangle>
                                <Rectangle Width="{Binding Path=FreeSpaceIndicator.Width}" 
                                           Fill="{Binding Path=FreeSpaceIndicator.Fill}"
                                           Height="25" Margin="0,3,3,3">
                                </Rectangle>
                            </DockPanel>
                        </Grid>
                        
                    </Border>
                    
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
